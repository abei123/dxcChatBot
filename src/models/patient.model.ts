// === MentorMe 2.x Login Model
// @name loginModel
// @desc The user model is a small interface that defines the properties of a user.
// @params none
// @returns none

export interface PatientModel {
  id: string,
  name: string,
  gender: string,
  birthDate: string
}
